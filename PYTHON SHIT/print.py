count = 1

# Loop until 15 multiples are printed
while count <= 15:
    
    # Calculate the current multiple of 3
    multiple = 3 * count
    
    # Print the multiple on the same line
    print(multiple, end=" ")
    
    # Move to the next number
    count += 1
